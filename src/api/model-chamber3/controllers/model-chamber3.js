'use strict';

/**
 * model-chamber3 controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::model-chamber3.model-chamber3');
